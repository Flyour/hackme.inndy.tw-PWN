1、软件的使用测试是很重要的。
    在看程序的源码之前，我想先去看一下它的功能，然后随便输入一个index 13，结果发现了数组越界漏洞。模糊测试的意义就在于超出常规的
测试。所以在拿到一个题后，先去用以下，明白他的功能，顺便模糊测试，看有没有漏洞。

2、经过反编译后，仔细看每一个自定义函数的作用。
    比如这道题目里面的call_me_maybe ，这是一首歌名，也是一个自定义的函数名，而这个自定义函数的作用就是调用shell。知道了这个函数，我们就不用辛苦的再去传入字符串，调用system函数了。

3、这道题还是有很多地方不懂， 比如我自己写的exp， gets, pop_ret, bss_addr, system, pop_ret, bss_addr, 但是经过调试发现，就是不能通过gets把/bin/sh字符串传入bss段里面，不知道为啥。
